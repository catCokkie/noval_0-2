# 验证与测试场景（归档说明版）

## 1. 主库对齐检查（只读）

- 当前主库为 `noval_structure`。
- `noval_generate_plan/plan` 仅用于历史回溯，不作为执行入口。
- 若归档内容与主库冲突，以主库为准。

## 2. 去重测试

- `deepseek/gemini/qwen` 重复来源仅保留一份主记录。
- 其余重复源通过 `来源映射表.csv` 引用。

## 3. 一致性测试

- 人物动机或能力边界与章纲冲突，判定为 `P0`。
- R1-R3 任一违规，章节不得进入定稿层。

## 4. 伏笔测试

- `foreshadow_add` 与 `foreshadow_payoff` 必须与 `伏笔账本.yml` 状态流转一致。

## 5. 契约测试

- `07_提示词合约` 字段与 `08_自动化/schemas/*` 字段逐项对齐。

## 6. 闭环演练测试

- 抽1章执行：章纲 -> 场景骨架 -> 正文扩写 -> 连续性检查。
- 输出检查：通过率、P0数量、成本记录完整性。
